let
    var a := 0
    var grandparent := 1
    var parent := 3

    function fib(n:int) =(
        for i := 2 to n do (
            a := 3 * parent - grandparent;
            grandparent := parent;
            parent := a
        );
        a
    )

in
    a := fib(5)
    
end